'''
    with tf.Session() as sess:
        # initialize all the variables
        sess.run(init)
        # iterate till convergence
        done = False
        k = 0.0
        multiplier = 1e-3
        while not done:
'''

"""

"""